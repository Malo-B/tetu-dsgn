<svg width="400" height="120" xmlns="http://www.w3.org/2000/svg">
  <!-- TÊTU Logo SVG -->
  <defs>
    <style>
      .logo-text {
        font-family: 'Arial Black', 'Helvetica Neue', sans-serif;
        font-weight: 900;
        font-size: 72px;
        fill: #E74C3C;
        letter-spacing: -2px;
      }
    </style>
  </defs>
  
  <!-- Background (optional, can be removed) -->
  <rect width="400" height="120" fill="transparent"/>
  
  <!-- TÊTU Text -->
  <text x="200" y="80" text-anchor="middle" class="logo-text">TÊTU</text>
</svg>
